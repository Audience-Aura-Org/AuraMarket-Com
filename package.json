{
  "name": "aura-market-deploy",
  "version": "1.0.0",
  "scripts": {
    "build": "ls -R && cd aura-market/web && npm install && npm run build",
    "start": "cd aura-market/web && npm start"
  }
}
