{
  "name": "aura-market-monorepo",
  "version": "2.0.0",
  "scripts": {
    "build": "cd aura-market/web && npm install && npm run build && echo 'BUILD_COMPLETE'",
    "start": "node aura-market/web/.next/standalone/server.js"
  }
}
