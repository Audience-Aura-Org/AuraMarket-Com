{
  "name": "aura-market-monorepo",
  "version": "4.0.0",
  "scripts": {
    "build": "cd aura-market/web && npm install && npm run build",
    "start": "cd aura-market/web && (test -d .next || npm run build) && npm start",
    "deploy": "cd aura-market/web && npm install && npm run build && npm start"
  }
}
