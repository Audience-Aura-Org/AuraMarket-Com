export const metadata = {
  title: 'Official Stores | Auradime Directory',
  description: 'Discover verified premium merchants and boutique stores on the Auradime platform.',
};

export default function StoresLayout({ children }) {
  return children;
}
