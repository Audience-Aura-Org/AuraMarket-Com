export const metadata = {
  title: 'Official Stores | Aura Market Directory',
  description: 'Discover verified premium merchants and boutique stores on the Aura Market platform.',
};

export default function StoresLayout({ children }) {
  return children;
}
